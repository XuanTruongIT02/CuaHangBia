@model IEnumerable<CuaHangBiaOnline_62131086.Models.Loaibia_62131086>

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_LayoutAdmin.cshtml";
}

<h2>Quản lý Loại Bia</h2>

<p>
    @Html.ActionLink("Thêm mới Loại Bia", "Create")
</p>
<table class="table">
    <tr>
        <th>
            Mã Loại Bia
        </th>
        <th>
            Tên Loại Bia
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
<tr>
    <td>
        @Html.DisplayFor(modelItem => item.Maloai)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.Tenloai)
    </td>
    <td>
        <button class="btn btn-success">
            @Html.ActionLink("Sửa", "Edit", new { id = item.Maloai}, new { @style = "color:white;" })
        </button>
        <button class="btn btn-primary">
            @Html.ActionLink("Chi tiết", "Details", new { id = item.Maloai }, new { @style = "color:white;" })
        </button>
        <button class="btn btn-danger">
            @Html.ActionLink("Xóa", "Delete", new { id = item.Maloai }, new { @style = "color:white;" })
        </button>
    </td>
</tr>
}

</table>
